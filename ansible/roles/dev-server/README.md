# Providions DEV Server

## Prerequsite

1. `sudo update-alternatives --config editor` -> /usr/bin/vim.basic
2. `sudo visudo` -> `tying     ALL=(ALL) NOPASSWD:ALL`

## Manual

cat ~/.ssh/id_rsa.pub and setup on github
https://github.com/zhangtai/dotfiles
